# Assets & Icons

This directory contains application visual assets, window icons, and UI resources.
The application UI uses built-in Unicode security symbols and dynamic canvas elements for native rendering across platforms without requiring external image binary dependencies.
